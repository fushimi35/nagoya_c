<% @comments.each do |comment| %>
      <tr>
        <td><%= comment.user.name %></td>
        <td><%= comment.body %></td>
        <td><%= link_to "削除",  recipe_comment_path(comment, comment), method: :delete %></td>
      </tr>
    <% end %>